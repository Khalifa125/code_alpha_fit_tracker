name: iOS Build (unsigned IPA)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.29.0

      - name: Setup Xcode
        run: |
          sudo xcode-select -switch /Applications/Xcode_16.1.app 2>/dev/null || true
          xcodebuild -version

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Clean build artifacts
        run: flutter clean

      - name: Get dependencies
        run: flutter pub get

      - name: Update CocoaPods
        run: |
          gem install cocoapods -v 1.15.2 --no-document
          pod --version

      - name: Install iOS pods
        working-directory: ios
        run: |
          pod install --repo-update

      - name: Build unsigned IPA
        run: flutter build ipa --no-codesign --release

      - name: Verify build output
        run: |
          echo "=== Archive ==="
          ls -la build/ios/archive/ 2>/dev/null || echo "No archive directory"
          echo "=== IPA ==="
          ls -la build/ios/ipa/ 2>/dev/null || echo "No IPA directory"
          echo "=== App ==="
          ls -la build/ios/app/ 2>/dev/null || echo "No app directory"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: fit-tracker-ipa
          path: |
            build/ios/ipa/*.ipa
            build/ios/archive/*.xcarchive
          if-no-files-found: warn
